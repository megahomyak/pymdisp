from aiogram.types import Message
import m_tg_utils

bot = m_tg_utils.Bot(token=input("Enter the bot's token: "))

async def handle_message(waiter: pymdisp.Waiter)

@bot.dp.message()
async def _handle_message(message: Message):
    

bot.start()
